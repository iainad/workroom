# Swift formatting & linting for the Workroom macOS app (swift-format, via Scripts/).
# Building/running the app itself is Scripts/run.sh — see macapp/CLAUDE.md.
.PHONY: format lint

format:
	Scripts/format.sh

lint:
	Scripts/lint.sh
